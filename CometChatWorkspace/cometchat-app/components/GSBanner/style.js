import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  root: {
    position: 'relative',
    alignItems: 'center',
    margin: 32,
  },
  logo: {
    position: 'absolute',
    bottom: 12,
  },
});

export default style;
